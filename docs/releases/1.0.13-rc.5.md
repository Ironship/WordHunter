# Word Hunter 1.0.13-rc.5

Fifth release candidate of the 1.0.13 line.

## Changes since 1.0.13-rc.4

- **No code changes** — version bump only, so a freshly installed build
  is unambiguously identifiable on the device (About/version string
  shows `1.0.13-rc.5`). The fix set is identical to rc.4:
  Android image adding (`#262`, `#265`), flashcard AI notes (`#261`,
  `#266`), shuffled review queue (`#263`), upload compression (`#264`),
  backend accept-error resilience (`#267`).

## Verification

- Same code as rc.4 (tag `WordHunter1.0.13-rc.4`); `scripts/validate.sh`
  - "Validation complete." on this branch.
